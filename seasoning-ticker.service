[Unit]
Description=Seasoning ticker — precise timing for Waybar clock modules
Documentation=https://github.com/TaxiService/seasoning
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/seasoning-ticker
Restart=on-failure
RestartSec=1

# Environment
Environment="SEASONING_SIDES_INTERVAL=minute"

# Logging
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=default.target